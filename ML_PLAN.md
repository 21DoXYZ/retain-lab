# ML-план: удержание, депозит-прогрессия, винбэк

> Памятка по моделям до их сборки. Что делаем, какие данные нужны и что из них есть.
> Датасет: ClickHouse `retention`, период 2025-12-01 → 2026-06-04, валюта TRY, ~39 010 real-игроков.
> Ключ — `casino_player_id`. Всё считаем **только по `account_type='normal'`**.

---

## 0. Контекст и зачем (важные факты из данных)

- **Отток ~92%** на 30-дневном горизонте (cutoff 2026-05-09: из 4 911 активных ушли 4 536).
  Даже среди вовлечённых (≥2 дней) уходят ~88%.
- **64.7% игроков сыграли только 1 день** (one-and-done).
- Активность по месяцам падает: дек 756 → фев 9 969 → **март 12 718 (пик)** → апр 5 323 → май 2 807.
- **Вывод:** «предсказать кто уйдёт» почти бессмысленно (уходят почти все). Ценность ML — в
  переформулированных таргетах (повторный депозит, ранний слив, винбэк) и в **ранжировании по value×retainability**.

---

## 1. Почему CatBoost, а НЕ нейросеть (бэкпроп) — ответ на вопрос

Для **табличных** данных (строки фич: recency, частота, депозиты, провайдер, страна…):

| Критерий | CatBoost (бустинг) | Нейросеть (бэкпроп) |
|---|---|---|
| Качество на таблицах | **Почти всегда лучше** (Grinsztajn 2022, Kaggle) | Хуже, кроме огромных данных |
| Категории (провайдер/аффилиат/платёжка) | **Нативно**, без кодирования | Нужны embedding/one-hot |
| Объём данных | Тысячи строк — ок | Нужны десятки-сотни тысяч |
| Интерпретируемость | **SHAP / важность фич** (видно ПОЧЕМУ) | Чёрный ящик |
| Тюнинг/скорость | Быстро, мало настроек | Дольше, капризнее |

- **CatBoost = это и есть «обычный/стандартный» ML для таблиц.** Бэкпроп-нейросети — для картинок/текста/звука/длинных последовательностей.
- Нам интерпретируемость критична: цель — понять *почему* уходит и *какой бонус* дать.
- **Продвинутая опция на потом:** моделировать сырую последовательность ставок (RNN/Transformer по событиям).
  CatBoost на агрегированных ритм-фичах ловит ~90% этого дешевле. Берём только если CatBoost упрётся в потолок.
- Бейзлайны для сравнения: логрег / простое правило (для at-risk — «просрочка vs личный ритм»).

**Итог: строим на CatBoost. Нейросеть не нужна.**

---

## 2. Что есть в данных (инвентаризация)

| Таблица | Что даёт | Для чего |
|---|---|---|
| `users` (39k) | профиль, FTD-дата/сумма, аффилиат, страна, баланс-снимок, verified | базовые фичи, метка депозитора |
| `money_transactions` | депозиты/выводы/бонусы + время, статус, суммы, `payment_method` (теги `campaign:`), `notes` | депозит-цепочки, бонус-история |
| `game_transactions` (54M) | каждая ставка/выигрыш + время (UTC), `game_uuid`, провайдер, bet/win | вся динамика: recency, частота, деньги, ритм, игры |
| `game_sessions` | кол-во сессий, длительность | вовлечённость |

**Есть всё для таргетов и почти всех фич.** Чего НЕТ:
- ❌ **Названия игр** — только uuid → бонусы привязываем к провайдеру/uuid-кластеру, не к имени.
- ❌ **Канал связи** (telegram/whatsapp/marketing_consent — пусто) → можем сказать *какой* бонус, не *как доставить*.
- ⚠️ Нет чистой таблицы «бонус предложен→принят→результат» → реко бонусов сначала **по правилам**;
  аплифт-модель позже (если нароем сигнал в bonus-транзакциях / `campaign:`-тегах).
- ❌ Гео (всё TR), device (всё desktop), mode (всё real) — без вариации, бесполезны.

---

## 3. Общий фундамент: point-in-time сборщик (нужен ВСЕМ моделям)

Главная причина, почему текущая `player_features` НЕ годится для обучения: она — снимок на «сегодня»
по всей истории → **утечка таргета**. Нужен срез по времени.

- Параметры: дата среза **T** + горизонт **H**.
- Всё **до T** → фичи (recency-на-T, частота, деньги, ритм, игры, депозиты — «как было на T»).
- Окно **после T** (T, T+H] → метка.
- T двигаем по нескольким датам (несколько месячных срезов) → панель, больше строк.
- **Валидация по времени:** учим на ранних T, тестим на поздних (НЕ случайный сплит — иначе утечка).
- Истории хватает на **3–4 месячных среза** при H=30.
- Реализация: один параметризованный SQL/Python-скрипт `build_snapshot(T, H)` → таблица `train_*`.

---

## 4. Три модели = три бизнес-цели

### ① «Игрок сейчас сорвётся» — удержать вовремя
- **Население:** ставил в последние **7 дней** на T И ≥2 активных дней (есть что удерживать; one-day исключаем).
- **Таргет:** НЕ сыграет в (T, T+14] → «слетает».
- **Ключевые фичи:** личный ритм — текущий разрыв vs его медиана (просрочка); ставки за 7д vs пред-7д
  (спад моментума); recency, streak, deposit-recency, баланс, RFM-на-T, любимый провайдер, freespin/night доля.
- **Выход / действие:** P(слетит) × ценность → список «удержать сейчас», топ по value×risk → оффер.
- **Данные:** ✅ есть всё (ритм уже считаем на карточке игрока).
- **Каверзы:** при H=30 даже вовлечённые уходят ~88% → берём **H=14** и население «активен на этой неделе».
  Ценность — в ранжировании по retainability×value, а не в точности per se.

### ② Сделает ли 2-й / 3-й / 4-й депозит
- **Население:** сделал депозит #N, но не #(N+1). **Старт: #1→#2** (главный рычаг — повторные платят).
- **Таргет:** сделает #(N+1) в 30 дней (event-time: T = FTD + несколько дней).
- **Фичи:** сумма FTD, скорость reg→FTD, поведение **первой сессии** (ставки, провайдеры, фриспины,
  выигрыш/проигрыш), платёжка, страна, аффилиат, полученные бонусы.
- **Выход / действие:** P(повторного деп) → нудж тем, кто близок; спец-оффер ценным с низким P.
- **Данные:** ✅ есть. **Самый сбалансированный и ценный таргет → строим первым.**

### ③ Ушёл → вернётся? стоит ли возвращать? какой бонус?
- **Население:** неактивен 30д+ на T, но был вовлечён.
- **3a. P(возврата):** вернулся (ставка) в (T, T+60].
- **3b. Стоит ли:** ожидаемая ценность при возврате × P(возврата) − стоимость бонуса.
- **3c. Какой бонус (по профилю):**
  - высокий freespin_ratio → фриспины на его любимом провайдере;
  - реал-грайндер → кэшбэк / деп-матч;
  - мелкий деп-тир → деньме/no-deposit бонус;
  - высокая ценность → персональный VIP-оффер.
- **Данные:** ✅ возвраты (по разрывам активных дней), ценность (депозиты/оборот), профиль игр.
  ⚠️ uplift «какой бонус сработает» — позже (сначала правила).
- **Проверить перед сборкой:** базовая доля возвратов (может быть низкой) и есть ли сигнал bonus→возврат.

---

## 5. Подход и оценка (для всех)
- Модель: **CatBoost** (категории нативно). Бейзлайн: логрег / правило.
- Метрики: **PR-AUC / AUC** (дисбаланс) + **калибровка** (для «стоит ли» нужны честные вероятности) + lift@K.
- Валидация: **временная** (ранние T → train, поздние → test).
- Интерпретация: **SHAP / feature importance** — чтобы видеть драйверы и выбирать действие.

---

## 6. Порядок сборки
1. **Point-in-time сборщик** (фундамент, §3).
2. **Модель ② (2-й депозит)** — первой: чистый таргет, max ценность, проверяет пайплайн.
3. **Модель ① (at-risk сейчас)** — на ритм-фичах.
4. **Модель ③ (винбэк + реко бонусов)**.
5. **Интеграция** скоров в борд: колонки `risk% / deposit% / winback% / рекоменд. бонус` у каждого игрока,
   сортировки и списки действий.

---

## 7. Открытые вопросы (решить до сборки)
1. **Горизонты:** at-risk H=14 ок? винбэк H=60 ок?
2. **«Ценность/VIP»** меряем по чему: оборот / сумма депозитов / GGR? (для «стоит ли возвращать»).
3. **Бонусы:** есть ли список реальных офферов казино (типы/условия), чтобы реко предлагал существующие?
4. **Окружение:** доустановить `catboost`, `scikit-learn`, `shap` в `.venv` (проверить наличие).
5. Стартуем с **②(2-й депозит)** или с **①(at-risk)**?
